# =============================================================================
# MEDICAL CHAIN MVP - ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git - it contains sensitive keys!

# =============================================================================
# HOW TO GET API KEYS - STEP BY STEP GUIDE
# =============================================================================

# 1. ALCHEMY/INFURA (Sepolia RPC URL)
#    - Go to: https://www.alchemy.com/
#    - Sign up for free account
#    - Create new app → Select "Ethereum" → Select "Sepolia"
#    - Copy HTTP URL (looks like: https://eth-sepolia.g.alchemy.com/v2/XXXXX)
#    - Free tier: 300M compute units/month (plenty for testing)
SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_API_KEY

# Alternative: Use Infura
# SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_KEY

# 2. WALLET PRIVATE KEY (MetaMask)
#    - Install MetaMask: https://metamask.io/
#    - Create wallet (save seed phrase securely!)
#    - Click account icon → Account details → Export private key
#    - IMPORTANT: Use a dedicated test wallet, never use mainnet funds wallet!
#    - Get Sepolia ETH from: https://sepoliafaucet.com/ (0.5 ETH/day)
#    - Format: 64 hex characters (0x + 62 chars) or just 64 chars
PRIVATE_KEY=your_wallet_private_key_here_without_0x_prefix

# 3. ETHERSCAN API KEY (Optional - for contract verification)
#    - Go to: https://etherscan.io/
#    - Register account → API Keys → Add
#    - Free tier: 5 calls/second
ETHERSCAN_API_KEY=your_etherscan_api_key

# 4. PINATA (IPFS Storage)
#    - Go to: https://www.pinata.cloud/
#    - Sign up for free account
#    - API Keys → New Key → Admin access
#    - Copy API Key and Secret
#    - Free tier: 500 files, 1GB storage
PINATA_API_KEY=your_pinata_api_key
PINATA_SECRET_KEY=your_pinata_secret_key

# =============================================================================
# CONTRACT ADDRESS (Auto-filled after deployment)
# =============================================================================
# Leave empty initially, run: .\setup.ps1 -DeployContract to auto-fill
# Or manually deploy and paste address here
CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000

# =============================================================================
# DJANGO CONFIGURATION
# =============================================================================
# Generate a new secret key: https://djecrety.ir/ or run:
# python -c "import secrets; print(secrets.token_urlsafe(50))"
DJANGO_SECRET_KEY=your_django_secret_key_change_this_in_production
DEBUG=True

# =============================================================================
# SERVICE URLS (Change if running on different ports/hosts)
# =============================================================================
ENCRYPTION_SERVICE_URL=http://localhost:8001
DJANGO_API_URL=http://localhost:8000

# =============================================================================
# FRONTEND ENVIRONMENT (Vite requires VITE_ prefix)
# =============================================================================
VITE_CONTRACT_ADDRESS=${CONTRACT_ADDRESS}
VITE_SEPOLIA_RPC_URL=${SEPOLIA_RPC_URL}
VITE_API_URL=http://localhost:8000
VITE_ENCRYPTION_API=http://localhost:8001

# =============================================================================
# ADVANCED CONFIGURATION (Optional)
# =============================================================================
# IPFS Gateway (alternative to Pinata)
# IPFS_GATEWAY=https://ipfs.io/ipfs/
IPFS_GATEWAY=https://gateway.pinata.cloud/ipfs/

# Gas price settings (Sepolia = testnet, keep default)
GAS_PRICE_GWEI=20

# Django allowed hosts (comma separated)
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS origins
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173